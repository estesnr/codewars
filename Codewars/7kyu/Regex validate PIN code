function validatePIN (pin) {
  //return true or false
  if(pin < 0) {
    return false
  }
  else if(pin.match(/^[0-9]+$/) == null) {
    return false
  }
  else if((pin.length == 4) || (pin.length == 6)) {
    return true
  }
  else {
    return false
  }
}
